import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function DropshippingManagementPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Gestión de Dropshipping</CardTitle>
        <CardDescription>
          Administra tus productos habilitados para dropshipping.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Esta página está en construcción.</p>
      </CardContent>
    </Card>
  );
}
