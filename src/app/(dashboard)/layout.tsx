import React from 'react';
// This file is part of a deprecated route structure.
// The dashboard is now located under /dashboard.
export default function DeprecatedLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
